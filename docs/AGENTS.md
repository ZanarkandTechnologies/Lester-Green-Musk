# `docs/AGENTS.md`

Scope: everything under `docs/`.

Rules:

- Keep docs concise and durable.
- Use `docs/HISTORY.md` for shipped repo changes worth preserving.
- Prefer append-only updates for historical logs.
