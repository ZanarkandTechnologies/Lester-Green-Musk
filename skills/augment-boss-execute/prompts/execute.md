# Execute Prompt

Copy and paste this into a new session to start an execution pass.

---

0a. Study `@memory/MEMORY.md` and `@HEARTBEAT.md`.
0b. Study `@skills/augment-boss-execute/SKILL.md`.
0c. Load the approved proposal ticket and latest comments/updates.

1. Execution mode: run only the approved actions from the proposal ticket.
2. Gather current context before touching links/merges/archives.
3. Execute actions, append `Update (timestamp)` to the same ticket.
4. Return status to `Review` unless explicitly told to mark done.

IMPORTANT: Execute only after explicit approval. Do NOT create new proposals in this session.
